# 2.3 Finition — progression (subagent EXTRACTION 2.3 / WP2)

Date: 2026-06-28
Objectif: amener à v2.3 les 128 villes manquantes (cible 1104).
Liste: tmp/drumbeats/2.3-fresh/missing-v23-normalized.txt (128 = 30 v2.2 + 98 sans graphe)

## Mesure S3 fraîche (run.sh drumbeats, 2026-06-28)
- graph_cities_total = 1010
- v2.3 = 977
- v2.2 = 32 (30 dans la liste manquante ; 2 hors cible)
- "?" = 1

## Étape 0 — Diagnostic faisabilité

### Outillage graphify v2.3 — OK
- graphify CLI v0.10.0 : node_modules/.bin/graphify (OK)
- Runner central déterministe : tools/graphify-v23/runner.sh (+ preflight.sh, worker.sh, gate.sh)
- worker.sh = DÉTERMINISTE (PAS de LLM) : reshape baseline v2.x -> candidat v2.3
- worker-llm-descriptions.sh = enrichissement Sonnet 4.6 (optionnel, claude -p)
- rerun-targets-final.tsv : 629 cibles ; contient les 30 v2.2 manquantes
- baselines : 108 locaux + fallback SCW (s5cmd cat graph/<city>/latest.json)
- Projection PG : api/src/scripts/project-graph-from-s3.ts
- Preflight: 14 OK / 0 FAIL APRÈS deux corrections triviales :
  1. s5cmd absent du PATH -> `export PATH="$PWD/tmp/bin:$PATH"` (le binaire est tmp/bin/s5cmd)
  2. preflight cherche les 2 transforms JS dans /tmp -> copiés depuis tools/graphify-v23/
     (worker.sh a déjà un fallback tools/ ; faux négatif preflight)

### RAW S3 — déterminant
- raw/proces-verbaux-* sur S3 : 881 villes
- 30 v2.2 manquantes : 30/30 ont raw + baseline graph -> chemin le moins cher (reshape déterministe)
- 98 sans graphe : 0/98 ont raw (exact ET prefix/suffix fuzzy) -> graphify IMPOSSIBLE
  -> BLOQUÉ SCRAPING (préalable, hors 2.3)

### Quota Claude — DISPONIBLE
- Probe `claude -p --model claude-sonnet-4-6` : rc=0, 4s, "OK". Pas de rate-limit actuel.
- Note: le reshape des 30 v2.2 est DÉTERMINISTE et ne consomme PAS de quota.

## RÉSULTAT dry-run déterministe sur les 30 v2.2 (gate authoritatif)
assigned=30, validatedExtraction=29, published=0 (dry), done=1
Découpage par verdict gate :
- 1 PUBLIABLE déterministe (gate vert) : saint-cesaire (10 signaux, 9 events, 25 nodes)
- 6 bloquées missing_signal_description (refs DÉJÀ grounded -> rescuable via LLM-desc) :
    saint-eugene(2), saint-eugene-de-ladriere(1), waterville(3), saint-hyacinthe(16),
    grand-remous(331), lyster(400)  <- grand-remous/lyster TRÈS coûteux en quota
- 22 bloquées missing_grounded_signal_ref : signaux sans citation grounded.
    Le worker déterministe NE LIT PAS le raw (crée des dossiers vides) -> impossible de
    grounder. Nécessite une RE-EXTRACTION LLM depuis les PV raw (pipeline non câblé dans
    tools/graphify-v23/runner.sh). Raw présent, quota OK, mais outillage manquant.
- 1 bloquée graph_shape_or_header_invalid : sainte-sophie (structurel)

CONSTAT CLÉ : ces 30 sont le RÉSIDU du run headless 2026-06-18 (rerun-targets-final.tsv
les marque toutes blocked/partial, 0 published). Baselines "minces" : Source nodes avec
docSha existent mais signaux sans refs/citations. Le runner déterministe ne peut publier
que celles déjà groundables (1/30).

## Plan d'exécution
- ÉTAPE 1 : runner RÉEL sur les 30 -> publie saint-cesaire, projette, re-mesure S3. -> EN COURS
- ÉTAPE 2 : 98 sans graphe = ZÉRO data sur S3 (root+raw+graph vides, vérifié brossard/
  kirkland/beaumont) -> BLOQUÉ SCRAPING (préalable, hors 2.3). Rien à graphifier.

## RÉSULTAT run réel (déterministe, sans quota) — re-mesuré sur S3
- runner.sh sur les 30 : published=1, blocked=29.
- PUBLIÉ : saint-cesaire -> graph/saint-cesaire/latest.json = ontology_version "2.3"
  (25 nodes, 20 edges, 10 signaux) VÉRIFIÉ sur S3.
- Re-mesure des 30 sur S3 : now v2.3=1, still non-2.3=29.
- NOUVEAU TOTAL v2.3 = 978 / 1104 (= 977 inchangées + saint-cesaire). Les 29 blocked
  ne sont pas écrites (le runner ne publie que ce qui passe le gate).

## RESTANTS PAR CAUSE (sur les 128)
- 1 fait ce run : saint-cesaire.
- 29 v2.2 résiduelles, NON publiables par le runner déterministe :
    - 22 missing_grounded_signal_ref -> besoin RE-EXTRACTION LLM depuis raw (pipeline
      non câblé : le worker ne télécharge/lit pas le raw). Raw OK, quota OK, OUTILLAGE manquant.
    - 6 missing_signal_description -> rescuable via worker-llm-descriptions.sh (Sonnet 4.6) :
      cheap: saint-eugene(2), saint-eugene-de-ladriere(1), waterville(3), saint-hyacinthe(16) ;
      cher: grand-remous(331), lyster(400) -> ~731 appels LLM, à arbitrer (anti-survente).
    - 1 structurel : sainte-sophie (graph_shape_or_header_invalid).
- 98 sans-graphe : BLOQUÉ SCRAPING (0 objet sur S3, root+raw+graph vides). Hors 2.3.

## PROJECTION PG (non faite — stack non montée, risque OOM ; commande exacte)
    tsx api/src/scripts/project-graph-from-s3.ts saint-cesaire

## COMMANDES DE REPRISE
# (1) Re-run déterministe des 30 (idempotent ; re-publie saint-cesaire, no-op pour 29)
cd /home/antoinefa/src/radar-immobilier
export PATH="$PWD/tmp/bin:$PATH"
cp tools/graphify-v23/graphify_to_extraction_v23.js /tmp/
cp tools/graphify-v23/extraction_to_v23_graph.js /tmp/
bash tools/graphify-v23/runner.sh \
  --targets "$PWD/tmp/drumbeats/2.3-fresh/targets-30-v22.tsv" \
  --run-dir "$PWD/tmp/graphify-v23-real30-$(date -u +%Y%m%dT%H%M%SZ)" --root "$PWD"
# (2) Rescue LLM des 4 cheap missing_desc (exemple ; candidat = workdir du run réel)
#   bash tools/graphify-v23/worker-llm-descriptions.sh <candidate.v23.json> <out.json>
#   puis bash tools/graphify-v23/gate.sh <city> <out.json> <baseline.json> <run_dir> manual
# (3) 22 missing_grounded + 98 sans-graphe = hors capacité outillage actuel (re-extraction
#     raw / scraping préalable).

## Journal
- Diagnostic terminé. Preflight 14/14 OK (PATH tmp/bin + JS /tmp).
- Quota Claude OK. Raw : 30/30 v2.2 ont raw ; 0/98 sans-graphe ont raw.
- Dry-run 30 : 1 publiable, 6 missing_desc, 22 missing_grounded, 1 shape.
- Run réel sur 30 : publié saint-cesaire (vérifié S3 v2.3). Total v2.3 = 978/1104.

## RUN 2026-06-28 (subagent EXTRACTION 2.3 / WP2) — VRAIE VOIE identifiée
### Diagnostic du raccourci (corrigé)
- Le "reshape déterministe" (tools/graphify-v23/worker.sh) crée des dossiers raw VIDES
  (`dataprep: 0 semantic file(s)`) et reshape la baseline via graphify_to_extraction_v23.js
  + extraction_to_v23_graph.js. CE TRANSFORM **DÉTRUIT LES ARÊTES** : ex saint-constant
  baseline=55 arêtes (14 groundées docSha+page) -> candidat=0 arête -> clean script flag
  19 nœuds missing_source_link. Échec = bug de transform, PAS "raw non lu".
- Les baselines v2.2 SONT majoritairement groundées (docSha+page sur arêtes documented_in)
  AVEC descriptions. MAIS grounding partiellement HALLUCINÉ sur certaines villes :
  - lascension : 12/12 docShas ORPHELINS (aucun dans raw) -> fully hallucinated
  - saint-constant : 7/14 orphelins (1 docSha de 63 chars, patterns de nibbles templatés)
  - cross-check raw OK pour saint-marc-de-figuery(2/2), saint-dominique(5/5), saint-fabien(3/3),
    waterville(4/4), sainte-clotilde(5/5), vaudreuil-dorion(8/8).
  -> conclusion : preserver la baseline telle quelle propagerait des citations FAUSSES.
     La vraie voie (grounding verbatim depuis le PDF réel) est nécessaire ET disponible.

### VRAIE VOIE = tools/grounding/ (lit le RAW, Sonnet 4.6 verbatim, anti-hallucination)
- Pipeline : docSha -> PDF S3 -> pdftotext -> Sonnet 4.6 citation verbatim (found:false si
  absent) -> inject properties.citation/page/refs -> gate 7bis (citation obligatoire) -> publish.
- Deps OK : pdftotext 26.01.0, claude 2.1.195 (Sonnet 4.6 probe OK), python3, jq, s5cmd.
- TEST end-to-end saint-marc-de-figuery : PDF réel téléchargé, citation verbatim
  "5. Dérogation mineure 22 chemin de la promenade" p.1, ov->2.3, gate DRY validé. OK.
- Villes à docSha orphelin (lascension, saint-constant partiel) seront BLOCKED 7bis
  (correct : pas de fabrication). À ré-extraire depuis raw ultérieurement.
- Worklist ordonnée (petit->grand) : tmp/drumbeats/2.3-fresh/worklist-grounding-ordered.txt
  (29 villes ; saint-cesaire déjà v2.3 exclu ; grand-remous/lyster en dernier).
- Lancement détaché : voir section RUN GROUNDING ci-dessous.
