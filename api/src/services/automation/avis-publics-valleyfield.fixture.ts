/**
 * The recorded real avis-publics fixture now lives in `@radar/sources` as the
 * single canonical fixture (No Legacy Fallback: no duplicate copy). Re-exported
 * here so existing automation-benchmark tests keep their import path.
 */
export { AVIS_PUBLICS_FIXTURE_HTML } from "@radar/sources";
